
# Python virtualenv
.venv/
.venv-linux/

# YOLO pretrained weights (auto-downloaded by ultralytics)
yolo*.pt

# Local secrets (never commit)
.env
.env.*
!.env.example

# Python bytecode
__pycache__/
*.py[cod]

# (Model-training Python tooling now lives in the sibling
# hooptrack-ball-detection/ repo — it has its own .gitignore.)
